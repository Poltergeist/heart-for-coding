function j(x, y, s){
  return function(){
    var d = s * 2,p = d+x;
    c.m(x,y);
    c.l(x+s,y-s);
    c.l(p,y);
    c.l(p,y);
    c.l(p,y+s);
    c.l(x+s,y+d);
    c.l(x,y+s);
    c.f();
  };
}
function i(x, y, s){
  return function(){
    c.m(x,y);
    c.l(x, y+s);
    c.l(x+s*2,y+s*3);
    c.l(x+s*4,y+s);
    c.l(x+s*4,y);
    c.f();
  };
}
function g(f){
  var k = s / 2;
  e(j(x+s, y+s, s), 200);
  e(j(x+s*2, y, s), 400);
  e(i(x, y, s), 400);
  e(n, 600);
  e(j(x+k, y-k, k), 0);
  if (f) {
    e(j(x, y, s), 0);
  }

}
function d(){
  m = a.pow(2, l(r()*8));

  x = r() * w;
  y = r() * h;
  c.ca();
  c.ba();
  c.fillStyle = 'hsl(' + l(r()*359) + ',99%,50%)';
  g(1);
  s += s;

}
function n(){
  if (s <= m) {
    g();
    s += s ;
    return;
  }
  s = 1;
  d();
}
var b = document.getElementById('C'),
  c=b.getContext('2d'),
  e = setTimeout,
  w = innerWidth,
  h = innerHeight,
  s = 1,
  a = Math,
  l = a.ceil,
  r = a.random,
  m, x, y;
for(p in c){
  c[p[0]+(p[6]||'')] = c[p];
}
b.width = w;
b.height = h;
n();
/**
 * a = math
 * b = canvas element
 * c = context
 * d = draw function
 * e = Timeout
 * f = first
 * g = draw heart
 * h = height
 * i = draw 3d
 * j = draw cube
 * k = half size
 * l = ceil
 */
